/** @type {import("prettier").Options} */
const config = {
  trailingComma: "all",
  semi: true,
  singleQuote: false,
  tabWidth: 2,
  useTabs: false,
  arrowParens: "avoid",
  printWidth: 120,
};

export default config;
